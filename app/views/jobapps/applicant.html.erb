<div class="container-job-index">
    <h1>All Applicants to my jobs</h1>
    <p>All applicants to your jobs should appear here</p>
    <% @applicants.each do |jobapp| %>
    <div class="job-card">
        <div class="job-image">
            <% if jobapp[:user].photo.attached? %>
                <%= cl_image_tag jobapp[:user].photo.key, class: "job-image" %>
            <% end %>
        </div>
        <div class="job-info">
            <h3 class="job-card-title"><%= jobapp[:user].fname %></h3>
            <p class="job-card-description"><%= jobapp[:job].job_title %></p>
            <div class="flex">
                <p class="job-card-description padding-right">Applicant:<%= jobapp[:user].fname %></p>
                <p class="job-card-description">Job Poster:<%= jobapp[:user].fname %></p>
            </div>
            <p class="job-card-price">Message: <%= jobapp[:jobapp].message %></p>
            <div class="job-view-button flex end card-icons">
                <div class="location-icon-width">
                    <i class="fas fa-map-pin icon"><span class="icon-text"><%= jobapp[:user].user_address %></span></i>
                </div>
                <%= link_to job_path(jobapp), method: :delete do %>
                    <i class="fas fa-trash icon"></i>
                <% end %>
                <%= link_to "View Job", job_path(jobapp), class: "button-card" %>
            </div>
        </div>
    </div>
    <% end %>
</div>